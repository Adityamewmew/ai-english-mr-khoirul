services:
  api:
    build:
      context: .
      dockerfile: Dockerfile
    ports: ["8001:8001"]
    env_file: .env
    restart: unless-stopped
  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    ports: ["5173:80"]
    depends_on: [api]
    restart: unless-stopped
