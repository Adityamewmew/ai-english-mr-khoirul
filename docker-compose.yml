services:
  app:
    build: .
    ports: ["8000:8000"]
    env_file: .env
    volumes: ["./:/app"]
    command: python main.py
  redis:
    image: redis:7-alpine
    ports: ["6379:6379"]
  qdrant:
    image: qdrant/qdrant:latest
    ports: ["6333:6333"]
