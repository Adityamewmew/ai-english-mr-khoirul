# Frontend — AI English Placement

Flow: Welcome (audio check) → Stage1 10Q → Stage2 adaptif 10Q → Writing+Speaking → Result radar.

Run:
```
cd frontend && npm install && npm run dev  # :5173 proxy /api -> :8000
```
Backend must run: `uvicorn src.api.routes:app --reload --port 8000` or `python main.py`
